[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
overflow-checks = false
debug-assertions = false

[profile.bench]
inherits = "release"

[package]
name = "lemonate"
version = "0.1.0"
edition = "2024"

[dependencies]
once_cell = "1.19"
rand = "0.9.2"
